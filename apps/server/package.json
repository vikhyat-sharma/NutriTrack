{
  "name": "@macrowise/server",
  "version": "0.0.1",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0",
    "@nestjs/jwt": "^10.0.0",
    "@nestjs/passport": "^10.0.0",
    "passport": "^0.6.0",
    "passport-jwt": "^4.0.0",
    "argon2": "^0.30.0"
  }
}
